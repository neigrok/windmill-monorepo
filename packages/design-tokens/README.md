# design-tokens

Empty — this package is this README and nothing else. Each surface owns its own copy of the raw
scales, all hand-mirrored: edit all three together.

- `web/src/styles/tokens/` — `colors.css`, `spacing.css`, `typography.css`, `radius.css`,
  `shadows.css`, `motion.css`, `palettes.css`, `fonts.css`
- `apps/ios/WindmillKit/Sources/WindmillPlatform/Tokens.swift`
- `apps/android/platform/src/main/kotlin/works/windmill/platform/design/Tokens.kt`
